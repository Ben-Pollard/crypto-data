package data_processors

//Defines allowable actions - apply after strategy generator
//Fixes bad status e.g. cancel orders that have been open too long
object RulesEngine {
  //max value open trades
  //max length order open
  //max number of open orders for one pair
  //max order value
  //max order value proportional to total
  //max value all open orders proportional to total
}
