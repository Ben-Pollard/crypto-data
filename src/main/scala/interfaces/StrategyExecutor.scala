package interfaces

import domain.Domain.Strategy

//takes a strategy from the strategy generator
//executes via the wallet / exchange interfaces
object StrategyExecutor {
  def apply(strategy: Strategy) : Unit = {

  }
}
